#include "Engineer2.h"
int Engineer::getCarPrice(Car* xx) {
	return xx->price;
}
int Engineer::getCarWheels(Car* xx) {
	return xx->wheels;
}
float Engineer::getCarSpeed(Car* xx) {
	return xx->speed;
}